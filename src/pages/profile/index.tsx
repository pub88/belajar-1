// import { useSession } from "next-auth/react";

export default function ProfilePage() {
    // const { data } = useSession();

    return (
        <>
            <img src="https://random.imagecdn.app/1500/300" />
        </>
    );
}